---
interface dataShape {
  userId: number;
  id: number;
  title: string;
  body: string;
}

let first: string;
let second: string;
let data: dataShape;

const postPost = async () => {
  const response = await fetch("https://jsonplaceholder.typicode.com/posts", {
    method: "POST",
    body: JSON.stringify({
      title: first,
      body: second,
      userId: 1,
    }),
    headers: {
      "Content-type": "application/json; charset=UTF-8",
    },
  });
  data = await response.json();
  console.log(first);
  console.log(second);
};
---

<script>
  const buttons = document.querySelectorAll("button.post-button");

  // Handle clicks on each button.
  buttons.forEach((button) => {
    button.addEventListener("click", () => {
      alert();
    });
  });
</script>

<h1>Post</h1>
<input class="border" type="text" name="first" />
<input class="border" type="text" name="second" />
<button class="post-button">post</button>
